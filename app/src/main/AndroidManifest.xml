<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comp3350.goodhabits">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoodHabits">
        <activity android:name=".Presentation.DetailActivity" />
        <activity android:name=".Presentation.HomeActivity" />
        <activity android:name=".Presentation.ProfileActivity" />
        <activity android:name=".Presentation.SettingsActivity" />
        <activity android:name=".Presentation.AllHabitsActivity" />
        <activity android:name=".Presentation.AddActivity" />
        <activity android:name=".Presentation.ProfileInputActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".Logic.HabitAlertReceiver"/>
    </application>

</manifest>